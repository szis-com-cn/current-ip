# 本地测试配置（accept_ip）
# 日志
log_file=./logs/webhook.log
log_level=INFO
log_max_size=10485760
log_backup_count=5

# Nginx（本地测试使用外部方式，不执行重载命令）
nginx_config_path=/nginx-data/allowed_ips.conf
nginx_reload_method=external

# 加密
secret_key=your_secret_key_here
max_timestamp_age=300