# SZIS-EDU-202507-002 服务器公网IP变动通知 任务说明书

- 任务编号：SZIS-EDU-202507-002
- 任务名称：服务器公网IP变动通知
- 任务金额：500元
- 任务时间：5天

## 任务说明

---

### **1. 项目背景与目标**

- **维度说明**：项目旨在开发一个轻量级的后台服务，用于解决公司部分部署在家庭或无固定公网IP环境下的开发/测试服务器的远程访问问题。当这些服务器的公网IP地址发生变化时，我们常常会失去连接，影响研发效率。
- **核心目标**：实现一个能够自动监测本机公网IP变化，并通过多种渠道（邮件、飞书、钉钉）实时通知相关人员的服务。同时，对外提供一个简单的API接口，以便其他内部系统可以按需查询该服务器的当前公网IP。
- **预期成果**：一个稳定、低资源消耗、易于部署和配置的独立应用程序。

---

### **2. 功能需求**

- **IP地址监测**：
    - 服务需定时（时间间隔可配置）通过访问免费第三方公共API获取本机的公网IP地址。
    - 服务需在本地持久化存储（如使用文件）当前IP地址，以便于比较。
    - 当检测到IP地址发生变化时，立即触发通知机制。
- **多渠道通知**：
    - 支持**邮件通知**：当IP变化时，向一个或多个指定邮箱发送通知邮件，邮件内容包含新的IP地址和更新时间。
    - 支持**飞书群机器人通知**：将IP变更信息推送到指定的飞书群。
    - 支持**钉钉群机器人通知**：将IP变更信息推送到指定的钉钉群。
    - 支持**自定义Webhook通知**：允许用户通过配置文件添加额外的HTTP POST请求，当IP变化时向指定URL发送POST请求，携带JSON格式的变更信息。
    - 所有通知渠道的开启/关闭以及相关凭证（如SMTP服务器信息、机器人Webhook地址）均需可通过配置文件进行设置。
- **API接口**：
    - 提供一个HTTP GET接口（如 `/api/current_ip`），用于查询当前最新的公网IP地址。
    - 为保证基本安全，该接口需支持通过简单的Token进行访问验证（Token在请求头或参数中传递）。
- **配置化管理**：
    - 需提供一个独立的配置文件（如 `.env` 文件），用于管理所有可变参数，包括：IP检查间隔、API服务端口、API访问Token、各通知渠道的配置信息等。代码中不允许硬编码任何敏感信息。

---

### **3. 技术栈要求**

- **后端语言**：推荐使用 **Bash**或**Python 3.12+**。
- **Web框架**：推荐使用 **Bash** 或 **Flask** 或 **FastAPI**，保持轻量级。
- **数据存储**：无需使用数据库，使用本地文本文件（如JSON格式）记录上一次的IP信息即可。
- **部署**：服务器环境为 Ubuntu 24.04，必须提供 `Dockerfile` 及 `docker-compose.yml` 文件，以便于快速、标准化的容器化部署。

---

### **4. 非功能需求**

- **性能**：服务应为轻量级，在常规空闲状态下，CPU占用率应极低，内存占用应小于100MB。
- **稳定性**：服务应能长期稳定运行，具备日志记录功能，关键操作（如IP获取失败、通知发送失败）需有日志输出。
- **安全性**：API接口必须有Token认证机制。所有敏感配置（如邮箱密码、机器人Token）必须通过配置文件或环境变量加载，严禁硬编码在代码中。
- **易用性**：需提供清晰的 `README.md` 文档，详细说明如何配置、部署和使用该服务。

---

### **5. 交付物与里程碑**

- 完成核心的IP监测与变化比对功能，能通过日志打印出IP变化情况。
- 完成所有通知渠道的开发与集成，并完成API接口开发。
- 系统测试、性能调优、打包部署文档
- 完成Docker化部署，撰写完整的项目文档（README），并交付全部源代码及配置文件示例。
- 最终交付源码、部署手册、API 文档、测试报告

---

### **6. 验收标准**

- 能够通过提供的 `docker-compose.yml` 文件一键启动服务。
- 配置文件工作正常，修改配置（如检查间隔、通知目标）后重启服务即可生效。
- 手动修改本地记录的IP文件以模拟IP变化后，所有已开启的通知渠道均能在设定的下一个检查周期内收到正确格式的通知。
- 调用API接口并提供正确的Token，能返回当前公网IP的JSON数据；使用错误的Token则访问被拒绝。
- 交付的 `README.md` 文档清晰明了，包含完整的配置项说明和部署步骤。
- 代码整洁，有必要的注释。

---

### **7. 知识产权与保密**

- 本次任务开发过程中产生的所有代码、文档及其他相关成果的知识产权完全归属于甲方（本公司）所有。
- 任务承接人需对任务内容、交付成果以及我方提供的任何测试信息（如API Token）严格保密，不得向任何第三方泄露。

---

### **8. 沟通与协作**

- **代码管理**：所有代码需通过Git进行版本控制，并提交至我方指定的代码仓库 [gitea.szis.dev](https://gitea.szis.dev/)，该仓库将自动同步到 Gitee、Github 开源仓库，请使用和 Gitee、Github 相同的邮箱在 Gitea 仓库注册账号。
- **沟通方式**：遇到需求疑问时，可随时通过即时通讯工具与我方技术对接人沟通。
- **甲方对接人**：项目技术经理，具体联系方式在任务启动后提供。

---

### **9. 预算与付款方式**

- 上线验收通过后支付全款。

---

### **10. 风险与变更管理**

- 本任务需求明确，原则上不接受在开发过程中的重大功能变更。

---

### **其他注意事项**

- 请自行准备用于测试的钉钉、飞书机器人Webhook地址以及测试邮箱账户。

---

### **关键字段示例**

> ```json
> {
>   "IP当前值": "114.34.123.45",
>   "上次IP值": "114.34.123.44",
>   "变更时间": "2025-07-14T15:30:00Z",
>   "通知结果": {"邮件": true, "飞书": false, "钉钉": true, "自定义Webhook1": true, "自定义Webhook2": true}
> }
> ```
